1. Which of the following arguments sets line style to solid in the plot() function?
a. solid_line = TRUE
b. line = 1
*c. lty = 1
d. lty = 6

2. Type the command to create a 2 x 2 graphic display matrix: {par(mfrow = c(2, 2))}

3. The RColorBrewer package is popular because it:
a. enhances the performance of the R graphics system
*b. allows one to easily create attractive color palettes
c. generates colorful graphs
d. adds color options to base R

4. Which of the following parameters are used to specify colors?
*a. col
*b. col.axis
*c. bg
d. color

5. The hist() function creates histograms. 
False
*True

6. Type the parameter name that will specify symbols to use for plotting points  {pch} 

7. What does the parameter lwd = 5 signify? 
a. A solid line 
b. A dotted line 
c. A solid line relative to default 
*d. A line five times wider then default 

8. Type the function to provide all available color names in R : {colors()} 

9. Type the function to suppress all axes automatically generated by the high-level plotting function: {axes=FALSE} 

10. True or False  
The abline()  function can be used to add reference lines, line type, color and width. 
*True 
False 

11. Which ways can be used to indicate the location of a legend within a graph? 
*a. Provide an x, y coordinate for the upper-left corner 
*b. Locator(1) and the mouse to interactively specify the location 
c) inset=
*d. topright

12. Select all functions that will combine several graphs into one overall graph. 
*a. par()
b. paste()
*c. layout()
d. mtext()
